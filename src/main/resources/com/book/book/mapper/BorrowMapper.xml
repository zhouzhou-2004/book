<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.book.book.mapper.BorrowMapper">
    <resultMap id="BookResultMap" type="com.book.book.model.pojo.Book">
        <id column="id" property="id" jdbcType="INTEGER"/>
        <result column="author" property="author" jdbcType="VARCHAR"/>
        <result column="isbn" property="isbn" jdbcType="VARCHAR"/>
        <result column="name" property="name" jdbcType="VARCHAR"/>
        <result column="pages" property="pages" jdbcType="INTEGER"/>
        <result column="price" property="price" jdbcType="DOUBLE"/>
        <result column="publish" property="publish" jdbcType="VARCHAR"/>
        <result column="publish_time" property="publishTime" jdbcType="VARCHAR"/>
        <result column="size" property="size" jdbcType="INTEGER"/>
        <result column="type" property="type" jdbcType="VARCHAR"/>
    </resultMap>

    <update id="updateBor" parameterType="map">
        update borrow
        <set>
            <if test="userId != null">
              user_id = #{userId},
            </if>
            <if test="bookId != null">
              book_id = #{bookId},
            </if>
            <if test="createTime != null">
             create_time = #{createTime},
            </if>
            <if test="endTime != null">
             end_time = #{endTime},
            </if>
            <if test="updateTime != null">
             update_time = #{updateTime},
            </if>
            <if test="ret != null">
             ret = #{ret},
            </if>
        </set>
        where id = #{id}
    </update>
    <!-- 获取所有图书信息 -->
    <select id="selectBooKAll" resultMap="BookResultMap">
        SELECT * FROM book
    </select>
    <!-- 根据关键字搜索图书 -->
    <select id="searchBooks" parameterType="string" resultType="com.book.book.model.pojo.Book">
        SELECT * FROM book WHERE name LIKE CONCAT('%', #{keyword}, '%');
    </select>
</mapper>